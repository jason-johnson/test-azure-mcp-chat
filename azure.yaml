name: azure-support-assistant
metadata:
  template: azure-support-assistant@0.0.1
services:
  api:
    project: ./api/prototype_copilot_sdk
    language: python
    host: containerapp
    docker:
      path: ./api/prototype_copilot_sdk/Dockerfile
      context: .
      remoteBuild: true