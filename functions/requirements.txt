# Microsoft Agent Framework (pre-release)
# NOTE: --pre flag is added in ADO pipeline pip install command
agent-framework
agent-framework-azurefunctions

# Azure Identity for managed identity authentication
azure-identity>=1.15.0

# Utilities
pydantic>=2.0.0
